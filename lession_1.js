alert("hello world inside index.id file")

let number = 10; //khai báo biến 
// (cấu trúc: từ khóa khai báo biến + tên biến = giá trị của biến)

number; //gọi lại giá trị đang nằm trong 1 biến

number = 20; //gọi lại giá trị và gán một giá trị mới vào biến

let hens = 20;

hens = hens + 20; //lưu trữ giá trị mới = 40.